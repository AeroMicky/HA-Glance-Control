node_modules
dist
*.ehpk
.env

# Internal task tracking (contains local IPs, session data)
.tasks/

# Screenshots may contain personal info from dev portals
screenshots/

# SDK design assets (large, not needed for build)
design-guidelines/

# Dev-only tools
icon-gen.html
screenshot.mjs

# Simulator raw screenshots (use `npm run screenshot <name>` to save properly)
glasses_*.png
app_*.png
screenshot.png
*.mov
